server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sanjayraj122/sanja-microservices-config
          clone-on-start: true
#          username: your-username # optional, if private repo
#          password: your-password # optional, if private repo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
